# ============================================================================
# Token Metrics Monitor - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your values
# Command: cp .env.example .env
# ============================================================================

# ----------------------------------------------------------------------------
# Database Configuration
# ----------------------------------------------------------------------------
# PostgreSQL connection settings
# If using Docker Compose, these defaults work out of the box
DB_HOST=localhost
DB_PORT=5432
DB_NAME=protocol_monitor
DB_USER=postgres
DB_PASSWORD=postgres

# ----------------------------------------------------------------------------
# Logging Configuration
# ----------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ----------------------------------------------------------------------------
# Slack Integration (Optional but Recommended for Alerts)
# ----------------------------------------------------------------------------
# Get your Slack Webhook URL:
# 1. Go to: https://api.slack.com/messaging/webhooks
# 2. Click "Create your Slack app"
# 3. Choose "From scratch"
# 4. Name your app (e.g., "Protocol Monitor")
# 5. Select your workspace
# 6. Click "Incoming Webhooks" → Toggle "On"
# 7. Click "Add New Webhook to Workspace"
# 8. Choose the channel for alerts
# 9. Copy the webhook URL below
#
# Direct link to create webhook: https://api.slack.com/apps?new_app=1
#
# Example format: https://hooks.slack.com/services/T00000000/B00000000/XXXXXXXXXXXXXXXXXXXX
SLACK_WEBHOOK_URL=

# ----------------------------------------------------------------------------
# Optional: DefiLlama API Configuration
# ----------------------------------------------------------------------------
# DefiLlama API is public and doesn't require an API key
# But if you want to use a custom endpoint or proxy, configure here:
# DEFILLAMA_API_BASE=https://api.llama.fi

# ----------------------------------------------------------------------------
# Optional: Custom Protocol Configuration
# ----------------------------------------------------------------------------
# To monitor additional protocols, add them to src/config.py
# Find protocol slugs at: https://defillama.com/protocols

# ----------------------------------------------------------------------------
# Production Deployment Settings (Advanced)
# ----------------------------------------------------------------------------
# Uncomment and configure for production deployments

# API Server
# API_HOST=0.0.0.0
# API_PORT=8000

# Grafana
# GF_SECURITY_ADMIN_USER=admin
# GF_SECURITY_ADMIN_PASSWORD=change_this_password

# Database (Production)
# For production, use a managed PostgreSQL service:
# - AWS RDS: https://aws.amazon.com/rds/postgresql/
# - Google Cloud SQL: https://cloud.google.com/sql/docs/postgres
# - DigitalOcean: https://www.digitalocean.com/products/managed-databases-postgresql
# - Supabase: https://supabase.com/database
#
# DB_HOST=your-production-db.region.rds.amazonaws.com
# DB_PORT=5432
# DB_NAME=protocol_monitor
# DB_USER=your_db_user
# DB_PASSWORD=your_secure_password
# DB_SSL_MODE=require

# ----------------------------------------------------------------------------
# Quick Start Checklist
# ----------------------------------------------------------------------------
# [ ] Copy this file: cp .env.example .env
# [ ] Set up Slack webhook (optional but recommended)
# [ ] For production: Change default passwords
# [ ] For production: Use managed database service
# [ ] Run: docker-compose up --build
# [ ] Access API: http://localhost:8000
# [ ] Access Grafana: http://localhost:3000
# ----------------------------------------------------------------------------
